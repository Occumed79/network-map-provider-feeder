# Render Background Worker is not the deployment target for this project.
# The feeder is intended to remain free-first, and Render worker setup was not available on the user's free path.
# Keep this file empty of services so Render Blueprints do not accidentally create paid infrastructure.
services: []
